# ==========================================
# Prefix
# ==========================================

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# ==========================================
# Windows & Panes
# ==========================================

bind-key c new-window -c "#{pane_current_path}"

# split panes using | and -
bind-key | split-window -h
bind-key - split-window -v
unbind '"'
unbind %

# switch panes using Alt-hjkl without prefix
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# don't rename windows automatically
set-option -g allow-rename off

# ==========================================
# Mouse & Keys
# ==========================================

# Enable mouse control
set -g mouse on

# Reduce escape key delay to prevent ESC interception
set -sg escape-time 10

# set vi key bindings in copy mode
setw -g mode-keys vi

# ==========================================
# Copy Mode
# ==========================================

bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -sel clip -i"

# In prefix mode, bind 'u' to enter copy mode and search backward for the prompt arrow
bind-key u copy-mode \; send-keys -X search-backward "➜"

# ==========================================
# Miscellaneous
# ==========================================

# reload config file
bind r source-file ~/.tmux.conf \; display "Reloaded!"
